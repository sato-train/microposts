<li id="micropost-<%= micropost.id %>">
  <%= link_to gravatar_for(micropost.user, size: 50), micropost.user %>
  <span class="user"><%= link_to micropost.user.name, micropost.user %></span>
  <span class="content">content - <% micropost.content %></span>
  <span class="timestamp">
    <!--Posted <%#= time_age_in_words(micropost.created_at) %> ago.-->
    Posted <%= micropost.created_at %> ago.
    <% if current_user == micropost.user %>
      <%= link_to "delete", micropost, method: :delete, data: { confirm: "You sure?"} %>
    <% end %>
  </span>
</li>

<!--
      <%#= link_to "delete", micropost, method: :delete, data: { confirm: "You sure?"} %>
      resources: micropost と設定してあるため、上記のように書いても自動的にurlが生成される。
      <%#= link_to "delete", micropost_path(micropost), method: :delete, data: { confirm: "You sure?"} %>
-->


