<% if @feed_items.any? %>

  <%#= page_entries_info @feed_items %>
  <!-- 日本語定義では正しく表示できない -->

  <ol class="microposts">
    <%= render @feed_items %>
  </ol>

  <%= paginate @feed_items %>

<% end %>
